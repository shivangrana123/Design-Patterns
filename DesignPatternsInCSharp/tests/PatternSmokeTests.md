# Smoke Test Plan

Manual checks for quick validation:
- Ensure every folder in `docs/PATTERNS.md` exists.
- Ensure each pattern folder contains `README.md` and `Example.cs`.
- Validate sample files use consistent namespaces.
